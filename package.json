{
  "name": "@minhchi1509/facebook-profile-cleaner",
  "description": "Clean your Facebook profile by removing all posts, comments, likes, and reactions, following, sent friend requests, and more...",
  "version": "1.1.2",
  "main": "dist/index.cjs",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && npm run esbuild && npm run build:dts",
    "esbuild": "bun esbuild.config.ts",
    "build:dts": "dts-bundle-generator -o dist/index.d.ts src/main.ts",
    "publish:patch": "npm run build && npm version patch --no-git-tag-version && npm publish --access public",
    "publish:minor": "npm run build && npm version minor --no-git-tag-version && npm publish --access public",
    "publish:major": "npm run build && npm version major --no-git-tag-version && npm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.25.11",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@fast-csv/format": "^5.0.2",
    "@fast-csv/parse": "^5.0.2",
    "axios": "^1.7.8",
    "dayjs": "^1.11.13",
    "fast-csv": "^5.0.2",
    "tslib": "^2.8.1"
  },
  "keywords": [
    "facebook",
    "facebook-profile-cleaner"
  ],
  "author": "minhchi1509",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/minhchi1509/facebook_profile_cleaner"
  },
  "homepage": "https://github.com/minhchi1509/facebook_profile_cleaner"
}
